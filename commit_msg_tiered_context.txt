feat(context): implement tiered context replacement (graphify-inspired)

- Add src/contextTiers.ts — shared type definitions and replaceTier() logic
  * ContextOrigin type ('ast' | 'semantic') for provenance tracking
  * ContextNode interface with _origin marker
  * replaceTier() function — tier-scoped replacement preserving unchanged nodes
  * createContextNode / createAstNode / createSemanticNode helpers

- Update src/stateManager.ts:
  * Add _origin?: ContextOrigin to StateEntry interface
  * Add setWithTier(key, value, origin) method for provenance-aware state setting
  * Add getByOrigin(origin) method — retrieve entries by tier
  * Add static mergeStateWithTiers(oldEntries, newEntries) — bulk tier replacement

- Update src/tools/contextManagementTools.ts:
  * Import contextTiers module
  * Add _origin?: ContextOrigin to ContextEntry interface
  * Implement entriesToNodes() / nodesToEntries() conversion helpers
  * Implement mergeEntriesWithTiers(oldEntries, newEntry) — tier-aware merging
  * Update addEntry() to use mergeEntriesWithTiers (backward compatible fallback)

- Update jest.config.cjs:
  * Add moduleNameMapper entry for ./contextTiers.js → src/contextTiers.ts

Verification: tsc --noEmit passes, all 23 test suites pass (371 tests)