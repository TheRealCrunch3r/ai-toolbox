/** Manual mock for backgroundCommandTools — returns a single dummy tool */
export function registerBackgroundCommandTools(_config, _bg) { return [{ name: 'run_in_terminal', description: 'Mock run_in_terminal' }]; }
