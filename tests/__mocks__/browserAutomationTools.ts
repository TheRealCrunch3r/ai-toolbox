/** Manual mock for browserAutomationTools — returns a single dummy tool */
export function registerBrowserTools(_config) { return [{ name: 'browser_open_page', description: 'Mock browser_open_page' }]; }
