/** Manual mock for cleanupBackupsTool — returns a single dummy tool */
export function registerCleanupBackupsTool(_config) { return [{ name: 'cleanup_backups', description: 'Mock cleanup_backups' }]; }
