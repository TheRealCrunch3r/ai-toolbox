/** Manual mock for contextManagementTools — returns a single dummy tool */
export function registerContextManagementTools(_config, _sm) { return [{ name: 'get_context_memory', description: 'Mock get_context_memory' }]; }
