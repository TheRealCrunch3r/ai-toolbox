/** Manual mock for dataVisualizationTools — returns a single dummy tool */
export function registerDataVisualizationTools(_config) { return [{ name: 'generate_chart', description: 'Mock generate_chart' }]; }
