/** Manual mock for markdownPreviewTools — returns a single dummy tool */
export function registerMarkdownPreviewTools(_config) { return [{ name: 'markdown_preview', description: 'Mock markdown_preview' }]; }
